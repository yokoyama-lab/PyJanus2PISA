// Own stress test: a procedure called twice (so it is not inlined) and then
// uncalled; exercises the procedure call protocol (SWAPBR / return offset) on
// both interpreters and the inverted companion procedure for `uncall`.
int x
int y
procedure bump
  x += 1
  y += x
procedure main
  call bump
  call bump
  uncall bump
